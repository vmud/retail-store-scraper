# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
brotli>=1.1.0  # Required for Brotli decompression (T-Mobile uses Content-Encoding: br)

# Async support for concurrent scraping
aiohttp>=3.9.0
aiofiles>=23.2.0

# Dashboard
flask>=3.0.0

# Configuration
pyyaml>=6.0.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-flask>=1.3.0

# Development (optional)
gitpython>=3.1.40  # For PR creation script
watchdog>=3.0.0    # For file watcher
pre-commit>=3.5.0  # For pre-commit hooks (3.5.0 is last version supporting Python 3.8)
