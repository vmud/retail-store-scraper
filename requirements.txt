# Core dependencies - pinned for reproducibility (#116)
requests==2.32.4
beautifulsoup4==4.12.2
lxml==5.1.0
brotli==1.2.0  # Required for Brotli decompression (T-Mobile uses Content-Encoding: br)

# Async support for concurrent scraping
aiohttp==3.9.1  # Used in tests/uat/protocol.py for async API testing
aiofiles==23.2.1

# Export formats
openpyxl==3.1.2  # Excel export support

# Cloud storage
google-cloud-storage==2.14.0  # GCS integration for backup/sync

# Performance
ijson==3.2.3  # Streaming JSON parser for memory-efficient change detection (#65)

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0  # Load .env file for environment variables

# Testing
pytest==7.4.3
pytest-mock==3.12.0
pytest-asyncio==0.23.2
pytest-cov==4.1.0

# Code quality & formatting
black==24.1.1      # Code formatter
isort==5.13.2      # Import sorter
mypy==1.8.0        # Type checker
types-requests==2.31.0.20240125  # Type stubs for requests
types-PyYAML==6.0.12.12          # Type stubs for PyYAML

# Security
defusedxml==0.7.1  # Safe XML parsing (prevents XXE attacks)
bandit==1.7.7      # Security linter

# Development (optional)
gitpython==3.1.41  # For PR creation script (CVE-2023-40590 fix)
watchdog==3.0.0    # For file watcher
pre-commit==3.5.0  # For pre-commit hooks (3.5.0 is last version supporting Python 3.8)
